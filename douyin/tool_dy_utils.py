import pandas as pd
import re
import tool_time
import numpy as np


def 网友评论解析(line: str) -> list[dict]:
    """
    解析单行网友评论文本，提取关键信息并返回字典列表

    参数:
        line: 单行评论文本，包含网友信息、行为、时间、bounds box等

    返回:
        list[dict]: 包含以下字段的字典列表：
            - 网友名称: str，网友的昵称（无则为空字符串）
            - 评论内容: str，评论/回复的具体内容（无则为空字符串）
            - 是否需回复: bool，是否包含"回复评论 回复,按钮"特征
            - left: int，bounds box左坐标
            - top: int，bounds box上坐标
            - right: int，bounds box右坐标
            - bottom: int，bounds box下坐标

    Doctest测试用例:
    >>> # 测试1：仅包含bounds box的空行
    >>> 网友评论解析(" (0, 2167, 1080, 2236)")
    {'网友名称': '', '评论内容': '', '是否需回复': False, 'left': 0, 'top': 2167, 'right': 1080, 'bottom': 2236}
    >>> # 测试2：赞了你的评论，无回复按钮
    >>> line2 = "魏晓樱Queen的头像 魏晓樱Queen 赞了你的评论 1分钟前 (0, 204, 1080, 447)"
    >>> 网友评论解析(line2)
    {'网友名称': '魏晓樱Queen', '评论内容': '', '是否需回复': False, 'left': 0, 'top': 204, 'right': 1080, 'bottom': 447}
    >>> # 测试3：评论了你，包含回复按钮（需回复）
    >>> line3 = "唯有欢喜的头像 唯有欢喜 粉丝 评论了你: 新年快乐 1分钟前 回复评论 回复,按钮 赞 赞,按钮 (0, 447, 1080, 709)"
    >>> res3 = 网友评论解析(line3)
    >>> res3['网友名称'], res3['评论内容'], res3['是否需回复']
    ('唯有欢喜', '新年快乐', True)
    >>> res3['left'], res3['top'], res3['right'], res3['bottom']
    (0, 447, 1080, 709)
    >>> # 测试4：带表情的评论内容
    >>> line4 = "花木蓝的头像 花木蓝 评论了你: 我也不知道有啥用[大笑][大笑] 23分钟前 回复评论 回复,按钮 赞 赞,按钮 (0, 622, 1080, 1136)"
    >>> d = 网友评论解析(line4)
    >>> d['评论内容']
    '我也不知道有啥用[大笑][大笑]'
    >>> d["是否需回复"]
    True
    >>> # 测试5：作者回复的场景
    >>> line5 = "花木蓝的头像 花木蓝 作者 回复: 是的，人间烟火气息[大笑] 32分钟前 回复评论 回复,按钮 赞 赞,按钮 (0, 1379, 1080, 1711)"
    >>> res5 = 网友评论解析(line5)
    >>> res5['网友名称'], res5['评论内容']
    ('花木蓝', '是的，人间烟火气息[大笑]')
    >>> res5['是否需回复']
    False
    >>> # 测试6：名称含特殊字符的场景
    >>> line6 = "农村阿娟带娃记（新人起步）的头像 农村阿娟带娃记（新人起步） 赞了你的评论 7分钟前 (0, 1195, 1080, 1438)"
    >>> 网友评论解析(line6)['网友名称']
    '农村阿娟带娃记（新人起步）'
    >>> # 测试7：赞了你的图文场景
    >>> line7 = "花木蓝的头像 花木蓝 赞了你的图文 23分钟前 (0, 1136, 1080, 1379)"
    >>> 网友评论解析(line7)['评论内容']
    ''
    >>> 网友评论解析(line7)['是否需回复']
    False
    """
    # 初始化返回结果

    # 正则匹配行末尾的bounds box（捕获left/top/right/bottom）
    bounds_pattern = re.compile(r"\((\d+),\s*(\d+),\s*(\d+),\s*(\d+)\)$")
    bounds_match = bounds_pattern.search(line)

    # 提取bounds box坐标（默认0，防止匹配失败）
    left = int(bounds_match.group(1)) if bounds_match else 0
    top = int(bounds_match.group(2)) if bounds_match else 0
    right = int(bounds_match.group(3)) if bounds_match else 0
    bottom = int(bounds_match.group(4)) if bounds_match else 0

    # 去掉bounds box后的纯文本内容（用于提取其他信息）
    text_without_bounds = bounds_pattern.sub("", line).strip()

    # 1. 提取网友名称：匹配"的头像"后的名称（直到遇到粉丝/作者/赞了/评论了你/回复:等关键词）
    # name_pattern = re.compile(
    #     r"的头像\s+([^(\s+粉丝|\s+作者|\s+赞了|\s+评论了你|\s+回复:)]+)"
    # )
    name_pattern = re.compile(r"(.+?)的头像")
    name_match = name_pattern.search(text_without_bounds)
    网友名称 = name_match.group(1).strip() if name_match else ""

    # 2. 提取评论内容：优先匹配"评论了你:"，其次匹配"回复:"，无则为空
    content_pattern1 = re.compile(r"评论了你:\s*(.+?)\s+\d+分钟前")  # 评论你的场景
    content_pattern2 = re.compile(r"回复:\s*(.+?)\s+\d+分钟前")  # 作者回复的场景
    content_match1 = content_pattern1.search(text_without_bounds)
    content_match2 = content_pattern2.search(text_without_bounds)
    if content_match1:
        评论内容 = content_match1.group(1).strip()
    elif content_match2:
        评论内容 = content_match2.group(1).strip()
    else:
        评论内容 = ""

    # 3. 判断是否需要回复：精确匹配"回复评论 回复,按钮"
    是否需回复 = (
        "回复评论 回复,按钮" in text_without_bounds and content_match1 is not None
    )

    # 构建结果字典并添加到列表
    return {
        "网友名称": 网友名称,
        "评论内容": 评论内容,
        "是否需回复": 是否需回复,
        "left": left,
        "top": top,
        "right": right,
        "bottom": bottom,
    }


def 网友评论提取(job, results: list):
    data = []
    for e in results:
        line = f"{job.元素转字符串(e)}, {e.bounds}"
        # print(line)
        d = 网友评论解析(line)
        d["e"] = e
        d["秒数"] = tool_time.从字符串提取时间并转为秒(line, np.nan)[0]
        d["是否需回复"] = (
            False
            if not d.get("是否需回复")
            else d.get("网友名称") not in job.数据.定长队列
        )
        data.append(d)
    return pd.DataFrame(data)


def 提取链接(txt: str) -> list:
    """
    从输入文本中提取所有HTTP/HTTPS协议的链接

    Doctest单元测试用例：
    >>> 提取链接('1.76 复制打开抖音，看看【河马包包的菜园的作品】昭昭如愿，岁岁安澜# 热门 # 抖音美食推荐官 #... https://v.douyin.com/XrmRMhEVlbk/ 11/20 z@t.eb lcN:/ ')
    ['https://v.douyin.com/XrmRMhEVlbk/']
    >>> 提取链接('无链接的纯文本内容')
    []
    >>> 提取链接('多个链接测试：https://www.baidu.com 测试内容 https://www.google.com/index.html 结尾')
    ['https://www.baidu.com', 'https://www.google.com/index.html']
    >>> 提取链接('http开头的链接：http://example.com/123 abc')
    ['http://example.com/123']
    """
    # 正则表达式模式：匹配http/https开头，直到遇到空白字符为止的所有字符
    # https? 匹配http或https；:// 匹配协议后的固定分隔符；[^\s]+ 匹配任意非空白字符（直到空格结束）
    link_pattern = r"https?://[^\s]+"

    # 查找所有匹配的链接，返回列表
    matched_links = re.findall(link_pattern, txt)

    return matched_links


def 是否无内容(txt: str):
    """
    是否无内容 的 Docstring

    :param txt: 说明
    :type txt: str
    >>> 是否无内容('由于未提供具体短视频内容，无法生成精准评论~')
    True
    >>> 是否无内容(None)
    True
    >>> 是否无内容('内容')
    False
    """
    return re.search("(无法生成|链接)", txt) is not None if txt else True


def 是否为图文视频(txt: str) -> bool:
    """
    判断输入文本是否包含图文视频的核心特征（图片+数字+中文逗号+按钮）

    参数:
        txt: 待检测的文本字符串

    返回:
        bool: 包含图文特征返回True，否则返回False

    Doctest单元测试用例：
    >>> 是否为图文视频('关注 私信，按钮 赤壁付 未点赞，喜欢29，按钮 29 评论17，按钮 评论17，按钮 17 未选中，收藏1，按钮 1 分享，按钮 分享 音乐，@流浪的魂斗罗创作的原声，按钮 互相关注 咸宁市 | 玄素洞 @赤壁付 · 15分钟前 发布时间：15分钟前 【🌺小寒日·花间暖舟】载一船冬日的诗意停泊你眼前。... 展开 图片1，按钮 图片2，按钮 图片3，按钮 图片4，按钮 图片5，按钮 图片6，按钮 图片7，按钮 图片8，按钮 图片9，按钮 图片10，按钮 暂停视频，按钮')
    True
    >>> 是否为图文视频('纯视频内容，没有图片相关文字，暂停视频，按钮')
    False
    >>> 是否为图文视频('图片0，按钮 这是包含单数字的图文内容')
    True
    >>> 是否为图文视频('图片100，按钮 这是包含多数字的图文内容')
    True
    >>> 是否为图文视频('图片，按钮 没有数字的情况（非图文）')
    False
    >>> 是否为图文视频('')
    False
    >>> 是否为图文视频('图片abc，按钮 非数字字符（非图文）')
    False
    """
    # 正则表达式匹配核心特征：图片+1个及以上数字+中文逗号+按钮
    # \d+ 匹配1个或多个数字，确保是"图片+数字"的格式
    pattern = r"图片\d+，按钮"

    # 使用search查找是否存在匹配内容（找到1个即可判定）
    match_result = re.search(pattern, txt)

    # 有匹配结果返回True，无则返回False
    return match_result is not None


def 提取点赞与评论数(txt: str) -> tuple[int, int]:
    """
    从混合文本中提取点赞数（喜欢后接的数字）和评论数（评论后接的数字）

    单元测试示例：
    >>> 提取点赞与评论数("关注 私信，按钮 大漠雄鹰 未点赞，喜欢597，按钮 597 评论447，按钮 评论447，按钮 447 未选中，收藏收藏")
    (597, 447)
    """
    # 正则匹配"喜欢"后紧跟的数字（捕获组提取数字部分）
    like_match = re.search(r"喜欢(\d+)，按钮", txt)
    点赞数 = int(like_match.group(1)) if like_match else 0

    # 正则匹配"评论"后紧跟的数字（捕获组提取数字部分）
    comment_match = re.search(r"评论(\d+)，按钮", txt)
    评论数 = int(comment_match.group(1)) if comment_match else 0

    return 点赞数, 评论数


def has_interaction_keywords(text):
    """
    检测输入文本中是否包含指定的互动类关键词中的任意一个。

    指定关键词列表：串门 互粉 窜门 互助 互关 互赞 互访 回关
    匹配规则：严格匹配完整关键词（区分全角/半角、空格分隔等），只要包含任意一个即返回True。

    参数:
        text (str): 需要检测的文本字符串（空字符串会返回False）

    返回:
        bool: 包含任意一个关键词返回True，否则返回False

    示例:
    >>> has_interaction_keywords("我来串门了，有空一起聊")  # 包含"串门"
    True
    >>> has_interaction_keywords("互粉互关，一起涨粉")  # 包含"互粉"+"互关"
    True
    >>> has_interaction_keywords("窜门的朋友请留步")  # 包含"窜门"（注意和"串门"的区别）
    True
    >>> has_interaction_keywords("大家互相帮助，互助共赢")  # 包含"互助"
    True
    >>> has_interaction_keywords("点赞后记得回关哦")  # 包含"回关"
    True
    >>> has_interaction_keywords("互赞一下，谢谢啦！")  # 包含"互赞"（带标点）
    True
    >>> has_interaction_keywords("有空互相访问，互访增加活跃度")  # 包含"互访"
    True
    >>> has_interaction_keywords("今天天气不错，适合出门散步")  # 无任何关键词
    False
    >>> has_interaction_keywords("")  # 空文本
    False
    >>> has_interaction_keywords("关键词是互粉吗？是的")  # 关键词在句子中间
    True
    >>> has_interaction_keywords("回关")  # 仅包含单个关键词
    True
    >>> has_interaction_keywords("记得互助！")  # 关键词带感叹号
    True
    >>> has_interaction_keywords("我想要互访，你愿意吗？")  # 关键词在句子中
    True
    >>> has_interaction_keywords("串门和窜门都算吗？算的")  # 包含两个相似关键词
    True
    >>> has_interaction_keywords("互 粉 这样分开的不算")  # 关键词被空格分隔，不匹配
    False
    >>> has_interaction_keywords("互芬（错别字）不算")  # 关键词错别字，不匹配
    False
    >>> has_interaction_keywords("回 关")  # 关键词拆分，不匹配
    False
    >>> has_interaction_keywords('、伙伴计划 发布者：@墨墨妈(有关必回)，配文“每天进步一点点，终会成功!姐妹们一起加油!”')
    True
    """
    # 定义需要检测的核心关键词列表（严格按用户要求）
    interaction_keywords = [
        "串门",
        "互粉",
        "窜门",
        "互助",
        "互关",
        "互赞",
        "互访",
        "回关",
        "有关必回",
        "伙伴计划",
    ]

    # 遍历关键词列表，检查是否有任意一个出现在文本中
    # 一旦匹配到立即返回True，提升执行效率
    for keyword in interaction_keywords:
        if keyword in text:
            return True

    # 未匹配到任何关键词，返回False
    return False


# 运行doctest单元测试
if __name__ == "__main__":
    import doctest

    print(doctest.testmod(verbose=False, report=False))
